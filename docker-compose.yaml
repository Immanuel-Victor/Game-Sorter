services:
  redis:
    container_name: redis
    image: redis
    ports:
      - "6379:6379"
    networks:
      - game-sorter
  app:
    build: 
      context: .
    depends_on:
      - redis
    environment:
      STEAM_CLIENT_KEY: ${STEAM_CLIENT_KEY}
      STEAM_URL: ${STEAM_URL}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
    ports:
      - '${PORT}:${PORT}'
    networks:
      - game-sorter

networks:
  game-sorter: